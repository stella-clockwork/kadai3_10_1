○ユーザー管理テーブル
    ○仮登録時のメールアドレスを保持できるようにカラムを追加

○ユーザー登録ページ
    ○本登録時、仮登録に使ったメールアドレスを本登録後のテーブルにも追加

ログインページ
    パスワードリセットページへの誘導を追加

login_control
    パスワード・メールアドレス変更ページへの誘導を追加


パスワードリセットページ(新規) password_reset_form.php + reset_password.php
○
    if (仮idつきでアクセスされていない) {
        if (ユーザー名がPOSTされていない) {
            ユーザー名を聞く
        } else {
            ユーザー名に紐付いたメールアドレスに仮idつきメールを送信する
            (10分で仮idを無効化するatを発行する)
        }
    } else {
        仮idがリセット待機テーブルに載っているかを確認
        if (新パスワードが答えられていない) {
            新パスワードを聞く
        } else {
            パスワードを更新する
        }
    }


パスワード変更ページ(新規) password_change_form.php + change_password.php
    if (新パスワードが答えられていない) {
        旧パスワードは聞かず、新パスワードを要求
    } else {
        パスワードを更新する
    }

/*
メールアドレス更新ページ(新規)
    if (仮idつきでアクセスされている) {
        if (仮idが正しい) {
            if (ユーザー名か新メールアドレスが送信されていない) {
                ユーザー名と新メールアドレスを要求
            } else {
                ユーザー名が正しいか検証
                メールアドレスを更新
            }
        } else {
            FATAL
        }
    } else {
        FATAL
    }
*/
//